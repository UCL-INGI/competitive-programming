accessible: true
author: Aubry François
context: |
    The **maximum sum subarray problem** is the following:

    Given an array :math:`a` with :math:`n` integers, we want to find a subarray of :math:`a` such that the sum of its elements is as large as possible.

    Recall that subarray is a continuous range, you cannot skip some elements. Otherwise the problem would be trivial as one could just pick all positive elements.

    Example:

    .. image:: dp-intro/example_maxsubarray.png

    The goal of this task is to develop an :math:`O(n)` algorithm for this problem.

    Let :math:`dp[i]` denote the maximum sum we can make with the subarray formet by the first :math:`i` elements of :math:`a`,  :math:`a[0], a[1], \ldots, a[i]`.

    The answer to our problem is :math:`dp[n - 1]` since this state consider the whole array :math:`a`.

    In DP we need to relate the answer to one problem with the answer to smaller subproblems. How can we relate :math:`dp[i]` with :math:`dp[i - 1]`?
environment: python3
evaluate: best
groups: false
limits:
    output: '2'
    time: '30'
    memory: '100'
name: DP - Maximum sum subarray
network_grading: false
order: 1
problems:
    dp-maxarraysum-relation:
        choices:
        -   text: :math:`dp[i] = \max(dp[i - 1], dp[i - 1] + a[i])`
        -   valid: true
            text: :math:`dp[i] = \max(dp[i - 1], dp[i - 1] + a[i], a[i])`
        -   text: :math:`dp[i] = \max(dp[i - 1], a[i])`
        type: multiple-choice
        header: |-
            What is the relation between :math:`dp[i]` and :math:`dp[i - 1]`?

            **Hint:** Think about it in terms of choices, to build the solution of :math:`dp[i]` we either take element :math:`a[i]` or we don't.
        name: Subproblem relationship
        limit: 0
    dp1-maxarraysum-1:
        allowed_exts:
        - .java
        header: In this first subproblem we solve a slightly modified version of the
            problem.
        name: Maximum sum subarray sum ending at
        type: code-file
stored_submissions: 0
submission_limit:
    amount: -1
    period: -1
weight: 1.0
